export default function ProgramsPage() {
  // This page is required for the parallel routes to work correctly
  // The actual content is rendered through the @hero and @programsList slots
  // defined in the layout.tsx
  return null;
}
